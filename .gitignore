# Logs
logs
*.log
*.log.err
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Agent / tool scratch dirs (never part of the build)
.freebuff/
.kilo/

# Root-level patch scratch files
/*.patch

# Deno lockfile (npm/package-lock.json is the source of truth; Deno only
# runs Supabase edge functions and does not need a committed lockfile)
deno.lock

# Test artifacts
playwright-report/
test-results/

# TypeScript build info
*.tsbuildinfo

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env.*.local

# Supabase CLI machine state (linked project info is local-only)
supabase/.temp/
supabase/.branches/
supabase/.env
.vercel
